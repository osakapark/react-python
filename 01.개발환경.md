# 📦 Front‑End 프로젝트 초기 셋업 가이드
**Vite + React + TypeScript + Tailwind CSS + shadcn/ui + Zustand + immer + React‑Query + ESLint/Prettier**

---
## 📚 목차
---

## 1. VS Code 플러그인
| 플러그인 | 역할 |
|---------|------|
| **Prettier - Code formatter** | 코드 자동 포맷 |
| **ESLint** | 정적 분석·에러·경고 |
| **Error Lens** | 에디터 안에 ESLint/TS 오류 강조 |
| **Tailwind CSS IntelliSense** | Tailwind 클래스 자동완성·프리뷰 |

> 플러그인은 VS Code 마켓플레이스에서 직접 검색·설치하면 됩니다.

---

## 2. 프로젝트 생성 & 기본 패키지 설치
```bash
1 Vite 프로젝트(React + TS + ESLint) 만들기
npm create vite frontend

2 프로젝트 폴더 진입
cd frontend

3. 기본 의존성 설치 (Vite가 자동 실행)
npm install

4. 런타임 라이브러리 추가
npm i react-router   
npm i zustand
npm i immer
npm i @tanstack/react-query
npm i @tanstack/react-query-devtools

npm run dev
```
## 3. tailwindcss--prettier 설정
```json
npm i -D prettier prettier-plugin-tailwindcss
```
.prettierrc.json
```
{
    "_plugins": ["./node_modules/prettier-plugin-tailwindcss/dist/index.mjs"],
    "plugins": ["prettier-plugin-tailwindcss"],
    "tabWidth": 4,
    "useTabs": false
}
```

## 4. shadcn ui
### 🛠️ 설치 따라하기
- [👉 shadcn/ui 설치 가이드 (Vite)](https://ui.shadcn.com/docs/installation/vite#existing-vite-project)
- **설정**: `radix`, `nova`

### 🌐 Chrome 확장 프로그램
- [⚛️ React Developer Tools 설치하기](https://chromewebstore.google.com/detail/react-developer-tools/fmkadmapgofadopljbjfkapdkoienihi?hl=ko)
- [🛠️ Redux DevTools 설치하기](https://chromewebstore.google.com/detail/redux-devtools/lmhkpmbekcpmknklioeibfkpmmfibljd?hl=ko)


## 5 vite.config.ts
```javascript
import tailwindcss from "@tailwindcss/vite"
import react from "@vitejs/plugin-react"
import { defineConfig } from "vite"
import { resolve } from 'node:path'

// https://vite.dev/config/
export default defineConfig({
  plugins: [react(), tailwindcss()],
  resolve: {
    alias: {
      '@': resolve(import.meta.dirname, 'src')
    }
  },
})
```

## 5 eslint.config.js
```javascript
import js from "@eslint/js";
import globals from "globals";
import reactHooks from "eslint-plugin-react-hooks";
import reactRefresh from "eslint-plugin-react-refresh";
import tseslint from "typescript-eslint";
import { defineConfig, globalIgnores } from "eslint/config";

export default defineConfig([
    globalIgnores(["dist"]),
    {
        files: ["**/*.{ts,tsx}"],
        extends: [
            js.configs.recommended,
            tseslint.configs.recommended,
            reactHooks.configs.flat.recommended,
            reactRefresh.configs.vite,
        ],
        languageOptions: {
            globals: globals.browser,
        },

        rules: {
            "react-refresh/only-export-components": "off",
            /* 템플릿 코드에 추가된 부분 */
            "@typescript-eslint/no-unused-vars": "off",
            "@typescript-eslint/no-explicit-any": "off",
        },
    },
]);
```

tsconfig.app.json
```json
/* Linting */
//"noUnusedLocals": true,      <-- 실습을 위해 주석 처리
//"noUnusedParameters": true,  <-- 실습을 위해 주석 처리
"erasableSyntaxOnly": true,
"noFallthroughCasesInSwitch": true,

/* 템플릿 코드에 추가된 부분 : Path Alias */      
"paths": {
    "@/*": ["./src/*"]
}
```

tsconfig.json
```json
{
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ],

  /* 템플릿 코드에 추가된 부분 : Path Alias */
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"]
    }
  }
}
```

tsconfig.node.json
```json
/* Linting */
// "noUnusedLocals": true, <-- 실습을 위해 주석처리
// "noUnusedParameters": true, <-- 실습을 위해 주석처리
```
components.json
```
1.설정창 열기: 단축키 Ctrl + , (Mac은 Cmd + ,)를 누릅니다.
2.검색: 상단 검색창에 json.schemas를 입력합니다.
3.https://ui.shadcn.com/schema.json 추가
```
main.tsx
```tsx
import { createRoot } from "react-dom/client";
import "./index.css";
import App from "./App.tsx";
import { BrowserRouter } from "react-router";
import { QueryClientProvider, QueryClient } from "@tanstack/react-query";
import { ReactQueryDevtools } from "@tanstack/react-query-devtools";

const queryClient = new QueryClient();

createRoot(document.getElementById("root")!).render(
  <BrowserRouter>
    <QueryClientProvider client={queryClient}>
      <ReactQueryDevtools />
      <App />
    </QueryClientProvider>
  </BrowserRouter>
);

```
